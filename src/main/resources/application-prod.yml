spring:
  datasource:
    url: jdbc:h2:file:./data/forexdb;MODE=MySQL # 재기동 후 데이터 파일 유지
    driver-class-name: org.h2.Driver
    username: ${DB_USERNAME:sa} # 보안상 상용은 계정 정보는 환경변수로... (디폴트는 sa)
    password: ${DB_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: WARN
    com.switchwon.forexordersystem: INFO
  file:
    name: logs/forex-order-system.log

management:
  endpoint:
    health:
      show-details: never # 보안상 비공개